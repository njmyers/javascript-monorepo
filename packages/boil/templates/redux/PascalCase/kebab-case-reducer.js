import { camelCaseName } from '../args';

const initialState = {
  key: 'value',
};

function camelCaseReducer(state, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default camelCaseReducer;
