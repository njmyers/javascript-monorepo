import path from "path";

const defaults = {
  templatesDirectory: path.resolve(__dirname, "../templates"),
  extension: undefined,
  include: undefined,
  exclude: undefined
};

export default defaults;
