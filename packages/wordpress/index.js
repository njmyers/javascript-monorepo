'use strict';

function _interopDefault(ex) {
  return ex && typeof ex === 'object' && 'default' in ex ? ex['default'] : ex;
}

var Wordpress = _interopDefault(require('Wordpress'));

module.exports = Wordpress.default;
