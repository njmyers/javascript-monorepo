# Directory

Reads a directory.

```js
import directory from '@njmyers/directory';

directions('path', {
  absolute: boolean,
  mime: boolean,
  recursive: boolean,
});

// defaults
const defaults = {
  absolute: true,
  mime: false,
  recursive: false,
};
```
